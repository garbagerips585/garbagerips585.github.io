.DS_Store
.claude/
Thumbs.db
*.log

# Never commit the YouTube API key.
.env
.env.*
!.env.example
.cache/

# import-sheet.mjs writes this as a temp and deletes it; a crashed run leaves
# a 0-byte file behind, and it was staged once already.
.sheet-import.csv
